extends layout
block page
  - var menu = 'signup'
block content
  .col-md-4.col-md-offset-4
    h2 회원가입
    div= error
    div
      form(action="/signup", method="POST")
        p
          input.form-control(name="email", type="text", placeholder="아이디 입력")
        p
          input.form-control(name="password", type="password", placeholder="비밀번호 입력")
        p
          input.form-control(name="password", type="password", placeholder="비밀번호 확인")
        p
          input.form-control(name="name", type="text", placeholder="닉네임 입력")
        p
          input.form-control(name="age", type="number", placeholder="나이 입력")
        p
          button.btn.btn-lg.btn-primary.btn-block(type="submit") Sign up
        br
        p
          | 이미 계정이 있으신가요? 
          a(href='/') 홈으로